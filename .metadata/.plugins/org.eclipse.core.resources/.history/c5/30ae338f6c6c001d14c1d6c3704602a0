package package1;

public class Access1 {

	//default = NO RESTRICTIONS
	//public = Works the same as default but must be written up front
	//private = ISOLATES FROM OTHER  FILES
	//protected = ALLOWS SHARING TO ALL CLASSES WITHIN THE PACKAGE BUT NO OTHER PACKAGES
	
	protected int hours = 3;
	protected int minutes = 47;
	
}
